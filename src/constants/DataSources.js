// Ordered by priority, higher takes precedence over lower
export const DEFAULT = 0    // Redux state
export const IDB_FAST = 1   // Partial Indexed DB
export const IDB = 2        // Indexed DB
export const API = 3        // TBA API
